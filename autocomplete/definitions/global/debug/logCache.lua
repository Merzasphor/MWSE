return {
	type = "value",
	description = [[A table containing all the output values of `debug.log()`. The table index is `path` with the line number appended, while the table key is the message.]],
	valuetype = "table",
}