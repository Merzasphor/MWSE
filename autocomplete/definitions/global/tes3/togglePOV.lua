return {
	type = "function",
	description = [[Forces a toggle of the player's POV the next simulation frame, and returns if the player was previously in 3rd person. Multiple calls in the same frame will not stack.]],
	returns = "was3rdPerson",
	valuetype = "boolean",
}