return {
	type = "function",
	description = [[Returns a safe handle for the object. To use the object use `:getObject()`. To check if it still exists use `:valid()`.]],
	arguments = { name = "object", type = "tes3object", description = "An object to make a safe handle for." },
	returns = "object",
	valuetype = "tes3object",
}
