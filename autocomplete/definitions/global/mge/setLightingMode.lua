return {
	type = "function",
	description = [[Sets the lighting mode used by MGE XE. The values passed can be used from the `mge.lightingMode.*` constants.]],
	arguments = {
		{ name = "mode", type = "number", description = "Maps from the `mge.lightingMode.*` constants." },
	},
}
