return {
	type = "value",
	description = [[The VFX's current keyframe timing. When this value exceeds the `endKeyTime`, the effect will either loop back to `beginKeyTime` or the effect will expire.]],
	valuetype = "number",
}
