return {
	type = "value",
	description = [[The minimum disposition that the info is filtered for. For journal-based dialogues, this is the same as the `journalIndex` property, which should be used instead.]],
	valuetype = "number",
}