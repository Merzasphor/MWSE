return {
	type = "method",
	description = [[Updates the world transform to point at a camera. Used automatically during rendering. This method is only needed when you need to get the world transform without rendering.]],
	arguments = {
		{ name = "camera", type = "niCamera" }
	},
}