return {
	type = "method",
	description = [[Returns true if either shift modifier key is pressed. It does not take into account caps lock.]],
	returns = {
		{ name = "isPressed", type = "boolean", description = "True if either shift key is pressed." },
	},
}