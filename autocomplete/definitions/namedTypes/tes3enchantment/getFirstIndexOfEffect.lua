return {
	type = "method",
	description = [[Returns the index of a first effect of a given effectId in the parent tes3enchantment object.]],
	arguments = {
		{ name = "effectId", type = "number", description = [[The effectID to perform a check for. Maps to tes3.effect.* constants.]] },
	},
	returns = "index",
	valuetype = "number",
}
