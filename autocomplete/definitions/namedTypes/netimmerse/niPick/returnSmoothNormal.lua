return {
	type = "value",
	description = [[If true and if vertex normals exist, then a unit-length normal vector is interpolated from the vertex normals and returned.  Otherwise, the returned normal vector is the unitized facet normal for the intersected triangle.]],
	valuetype = "boolean",
}