return {
	type = "value",
	description = [[The app-level source pixel data, if it exists (either because the texture was created from an NiPixelData object, or because createFromFilepath() has been called).]],
	valuetype = "niPixelData",
}