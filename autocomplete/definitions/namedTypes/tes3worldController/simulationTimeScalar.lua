return {
	type = "value",
	description = [[A scalar used for simulation time. At the start of every frame, the `deltaTime` is multiplied by this value. Doing this here is safer than doing it in another event.]],
	valuetype = "number",
}