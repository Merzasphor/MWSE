return {
	type = "class",
	description = [[A script context object, which allows variables to be get/set using the variable name.]],
	examples = {
		["..\\..\\tes3reference\\context\\hasCompanionShare"] = {
			title = "An example of indexing local script variables inside `tes3scriptContext` objects",
			description = "Companions usually have a mwscript script with variable named `companion` set to 1. This can be used to determine if a reference is player's companion or not.",
		}
	}
}
