return {
	type = "value",
	description = [[The filename (including the extension) of the mod that owns this object. It has `nil` value if the object was anything other than loaded from an ESP or ESM file.]],
	valuetype = "string",
	readOnly = true,
}
