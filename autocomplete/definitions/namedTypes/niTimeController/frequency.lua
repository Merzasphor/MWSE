return {
	type = "value",
	description = [[The animation controller's frequency value. The frequency is a scaling value to convert from update time units to keyframe time units, if necessary.
    ]],
	valuetype = "number",
}