return {
	type = "value",
	description = [[A number representing the axis that points at the lookAt object. `flip` determines which end of this axis points at the target.
`0` X axis
`1` Y axis
`2` Z axis]],
	valuetype = "number",
}