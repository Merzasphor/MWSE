return {
	type = "value",
	description = [[The duration of single iteration of the timer in seconds for timers of `timer.real` and `timer.simulate` types. It's in hours for `timer.game` timers.]],
	readOnly = true,
	valuetype = "number",
}
