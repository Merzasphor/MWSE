return {
	type = "method",
	description = "Gets the magnitude from the casting source for a given effect index.",
	arguments = {
		{ name = "index", type = "number", description = "The index in the effect list to fetch, between `0` and `7`." },
	},
	valuetype = "number",
}