return {
	type = "value",
	readOnly = true,
	description = [[Direct access to the tile's flags. Use `.isBartered`, `.isBoundItem`, or `.isEquipped` to access individual flags.]],
	valuetype = "number",
}