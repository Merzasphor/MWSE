return {
	type = "method",
	description = [[Tells the renderer that the object has changed. Should be called after changing textures, vertices, or other properties.]],
	arguments = {
		{ name = "flags", type = "number" },
	},
}