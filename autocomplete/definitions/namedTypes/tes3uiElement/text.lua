return {
	type = "value",
	description = [[The element's text. Text input can be read by accessing this property.

	tip: If your element's text is a number, you need to manually convert it to string using `tostring()`.]],
	valuetype = "string",
}