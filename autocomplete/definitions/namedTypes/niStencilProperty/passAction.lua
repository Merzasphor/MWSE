return {
	type = "value",
	description = [[The action that is taken in the stencil buffer when the stencil test passes and the pixel passes the Z-buffer test. See the table at the top for available actions.]],
	valuetype = "integer",
}