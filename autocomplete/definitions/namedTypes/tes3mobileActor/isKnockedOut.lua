return {
	type = "value",
	description = [[If `true`, the actor is knocked out. An actor can be knocked out if their fatigue has been reduced below zero.]],
	readOnly = true,
	valuetype = "boolean",
}
