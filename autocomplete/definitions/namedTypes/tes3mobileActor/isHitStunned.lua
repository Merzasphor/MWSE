return {
	type = "value",
	description = [[If `true`, the actor is affected by hit stun. This prevents the actor from initiating an attack, but not continuing and finishing an attack. It also prevents movement except for jumping.]],
	readOnly = true,
	valuetype = "boolean",
}
