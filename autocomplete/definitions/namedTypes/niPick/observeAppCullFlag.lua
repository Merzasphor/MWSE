return {
	type = "value",
	description = [[If true, the pick stops if it encounters an object with the 'app culled' flag set.  If false, the pick will continue over 'app culled' objects.
	]],
	valuetype = "boolean",
}