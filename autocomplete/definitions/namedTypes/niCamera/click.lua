return {
	type = "method",
	description = [[This method renders the currently-attached scene graph to the active renderer.]],
	arguments = {
		{ name = "something", type = "boolean", optional = true, default = false },
	},
}