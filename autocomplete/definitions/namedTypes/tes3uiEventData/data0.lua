return {
	type = "value",
	readOnly = true,
	description = [[Event-specific raw data values. For mouse events, these are the screen X and Y coordinates of the pointer. For keyboard events, data0 is the scan code.]],
	valuetype = "number",
}