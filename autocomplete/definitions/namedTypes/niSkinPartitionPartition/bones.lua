return {
	type = "value",
	description = [[The list of bone indices (that influence this partition) from the bones array in the parent `niSkinInstance` node.]],
	readOnly = true,
	valuetype = "integer[]",
}
