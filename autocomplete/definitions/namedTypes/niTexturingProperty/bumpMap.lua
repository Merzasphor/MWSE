return {
	type = "value",
	description = [[Gets or set the bump map. Can be set to nil to delete it. Note that doing so will free the memory of the map, causing all existing handles to it to become invalid.]],
	valuetype = "niTexturingPropertyMap|nil",
}