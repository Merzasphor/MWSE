return {
	type = "value",
	description = [[The texture apply mode. The texture apply mode determines how the vertex colors (whose generation are controlled via the NiVertexColorProperty) are blended with the filtered texture color.]],
	valuetype = "niTexturingPropertyApplyMode",
}